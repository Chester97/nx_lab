{
  "name": "foodity-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/foodity-app",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/foodity-app",
        "outputPath": "dist/apps/foodity-app"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/foodity-app"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "foodity-app:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "foodity-app:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "foodity-app:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "foodity-app:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/foodity-app/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/foodity-app/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
